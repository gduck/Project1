
 
    <!-- Fixed navbar -->
    <!-- <div class="container"> -->
    <nav id="header" class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- navbar-fixed-top -->
      <!-- <div class="container"> -->
      <div class="col-sm-10 col-sm-offset-1 col-xs-12">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#wantmaw-navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">
            <span>want</span><span class="frijole">MAW</span>
          </a>
        </div>
        <div id="wantmaw-navbar" class="collapse navbar-collapse">        
          <ul class="nav navbar-nav nav-pills right">
            <% if current_user %>
              <li class="<%= is_active?(root_path)%>"><%= link_to "Home", root_path, :method => :get %></li>
              <li ><%= link_to "Sign out", destroy_user_session_path, :method => :delete%></li>

              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                <% if current_user.first_name? %>
                  <%= "Hi #{current_user.first_name}!" %>
                <% else %>
                  <%=  current_user.email %>
                <% end %>
                <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li class="<%= is_active?(current_user)%>"><%= link_to "My profile", current_user %></li>
                  <li class="<%= is_active?(edit_user_path(current_user))%>"><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
                  <!-- <li><a href="#">Something else here</a></li> -->
                  <li class="divider"></li>
                  <li class="dropdown-header">Don't leave!</li>
                  <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete%></li>
                  <!-- <li><a href="#">One more separated link</a></li> -->
                </ul>
              </li>
            <% else %>
              <li class="<%= is_active?(new_user_registration_path)%>" ><%= link_to "Sign up", new_user_registration_path, :method => :get%></li>
              <li class="<%= is_active?(user_session_path)%>"><%= link_to "Log in", user_session_path, :method => :post%></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
  <!-- </div> -->
